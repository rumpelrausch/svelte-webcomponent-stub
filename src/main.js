/**
 * Register all non-referenced components here.
 * Rollup needs this to hand them over to the svelte compiler
 * which will then resolve all referenced components.
 * 
 * Exporting these imports is not necessary.
 */

import a from './components/public/webc-stub-button.svelte';
